<section class="container p-1">
<p style="color: green"><%= notice %></p>

<h1 class="text-danger">Minhas Receitas</h1>

<div id="recipes">
  <% @recipes.each do |recipe| %>
    <%= render recipe %>
    <p>
      <%= link_to "Show this recipe", recipe %>
    </p>
  <% end %>
</div>

<%= link_to "New recipe", new_recipe_path %>
</section>